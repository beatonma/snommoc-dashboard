import React from "react";
import { Icon, MaterialIcon } from "./symbol";

export function Loading(props) {
    return (
        <div className="loading">
            <MaterialIcon icon={Icon.unfeatured} />
        </div>
    );
}
